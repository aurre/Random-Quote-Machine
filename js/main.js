const colors = ['#42bcf4', '#95f441', '#f47041', '#f141f4', '#f4f141', '#af41f4', '#4155f4' ];

function getRandomColor(arr) {
    let index = Math.floor(Math.random() * Math.floor(arr.length));
    return arr[index]
}

function getQuote() {
    $.ajax({
        
    });
}